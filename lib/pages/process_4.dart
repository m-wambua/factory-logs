import 'package:flutter/material.dart';

class Process4Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Process 4'),
      ),
      body: Container(
        child: Text('This is Process 4'),
      ),
    );
  }
}
