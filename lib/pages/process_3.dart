import 'package:flutter/material.dart';

class Process3Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Process 3'),
      ),
      body: Container(
        child: Text('This is Process 3'),
      ),
    );
  }
}
