import 'package:flutter/material.dart';

class Process2Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Process 2'),
      ),
      body: Container(
        child: Text('This is process 2'),
      ),
    );
  }
}
