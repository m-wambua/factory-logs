import 'package:flutter/material.dart';

class Process1Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('Process 1'),
        ),
        body: Container(child: Text('This is Process 1 ')));
  }
}
